SET value, trim(input)
SET pieces, split(",", value)
SET without_first, remove_at(pieces, 0)
PRINT 0, join("-", pieces)
PRINT 0, element_at(without_first, 0)
PRINT 0, slice(value, 0, 5)
PRINT 0, slice(value, 6, 8)
PRINT 0, to_string(index_of("世", value))
PRINT 0, to_string(can_parse(value))
