name: Tests

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    permissions:
      contents: read
      checks: write

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 9.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore
    - name: Test
      run: dotnet test --no-build --verbosity normal --logger "trx;LogFileName=test-results.trx" --results-directory ./coverage --collect:"XPlat Code Coverage"
    - name: Publish test results check
      if: always()
      uses: dorny/test-reporter@v1
      with:
        name: Test Results
        path: ./coverage/**/test-results.trx
        reporter: dotnet-trx
    - name: Generate coverage summary
      if: always()
      uses: danielpalme/ReportGenerator-GitHub-Action@5.4.3
      with:
        reports: ./coverage/**/coverage.cobertura.xml
        targetdir: ./coverage/report
        reporttypes: MarkdownSummaryGithub
    - name: Publish coverage summary
      if: always()
      run: cat ./coverage/report/SummaryGithub.md >> "$GITHUB_STEP_SUMMARY"
    - name: Upload coverage artifact
      if: always()
      uses: actions/upload-artifact@v4.3.6
      with:
        name: coverage-report
        path: |
          ./coverage/**/coverage.cobertura.xml
          ./coverage/report/**
        retention-days: 30
